// array is a collection of similar data type multiple values
//it's indexing shows as marks[0],marks[1] etc 
// intializing of an array is--> int marks[]={marks[0],marks[1]....}

//WAP to input the array values

/*#include <stdio.h>

int main() {
    int marks[5];
    for (int i = 0; i < 5; i++)
    {
        printf("Enter the marks of student %d :",i+1);
        scanf("%d",&marks[i]);
    }
    
    return 0;
}*/
